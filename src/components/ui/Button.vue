<!--
  Button Component - StyleSnap
  
  Purpose: Reusable button component with multiple variants (primary, secondary, danger, etc.)
  
  Props:
  - variant: 'primary' | 'secondary' | 'danger' | 'ghost' (default: 'primary')
  - size: 'sm' | 'md' | 'lg' (default: 'md')
  - disabled: boolean
  - loading: boolean (shows loading spinner)
  - fullWidth: boolean (makes button 100% width)
  
  Usage:
  <Button variant="primary" @click="handleClick">Save</Button>
  <Button variant="danger" :loading="isDeleting">Delete</Button>
  
  Reference: requirements/frontend-components.md for design specifications
-->

<template>
  <!-- TODO: Implement button with variants and loading state -->
</template>

<script setup>
// TODO: Define props, emits, and button logic
// TODO: Add accessibility attributes (aria-label, aria-busy, etc.)
</script>

<style scoped>
/* TODO: Add component-specific styles (if not using pure Tailwind) */
</style>
